'use strict';

/**
 * Interface definition for objects that can perform entity serialisation.
 */
class EntitySerialiser {
    constructor() {
    }

    /**
     * Serialises the specified parameter into the entity representation.
     * @param {ParameterBase} parameter - The parameter to be serialised.
     */
    param(parameter) {
    }
}

module.exports = EntitySerialiser;
